{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Hologres Writer Schema",
  "description": "Hologres writer configuration for DataWorks Data Integration",
  "type": "object",
  "allOf": [
    {
      "$ref": "DIWriterBase.schema.json"
    }
  ],
  "properties": {
    "stepType": {
      "const": "holo",
      "description": "Hologres writer type"
    },
    "parameter": {
      "type": "object",
      "properties": {
        "column": {
          "type": "array",
          "description": "Column names to write to the table",
          "items": {
            "type": "string"
          },
          "minItems": 1
        },
        "datasource": {
          "type": "string",
          "description": "Datasource name in DataWorks"
        },
        "table": {
          "type": "string",
          "description": "Table name to write to"
        },
        "preSql": {
          "type": "array",
          "description": "SQL statements to execute before writing data",
          "items": {
            "type": "string"
          }
        },
        "postSql": {
          "type": "array",
          "description": "SQL statements to execute after writing data",
          "items": {
            "type": "string"
          }
        },
        "writeMode": {
          "type": "string",
          "description": "Write mode: insert, replace, or update",
          "enum": [
            "insert",
            "replace",
            "update"
          ]
        },
        "batchSize": {
          "type": "integer",
          "description": "Batch size for insert operations"
        },
        "username": {
          "type": "string",
          "description": "Database username"
        },
        "password": {
          "type": "string",
          "description": "Database password"
        },
        "jdbcUrl": {
          "type": "string",
          "description": "JDBC connection URL"
        },
        "trimString": {
          "type": "boolean",
          "description": "Whether to trim string values"
        },
        "ignoreDuplicate": {
          "type": "boolean",
          "description": "Whether to ignore duplicate key errors"
        },
        "sourceType": {
          "type": "string",
          "description": "Source type for streaming write"
        }
      },
      "required": [
        "column",
        "datasource",
        "table",
        "writeMode"
      ],
      "additionalProperties": true
    }
  },
  "additionalProperties": true
}
