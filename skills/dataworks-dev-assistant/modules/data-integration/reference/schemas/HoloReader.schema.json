{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Hologres Reader Schema",
  "description": "Hologres reader configuration for DataWorks Data Integration",
  "type": "object",
  "allOf": [
    {
      "$ref": "DIReaderBase.schema.json"
    }
  ],
  "properties": {
    "stepType": {
      "const": "holo",
      "description": "Hologres reader type"
    },
    "parameter": {
      "type": "object",
      "properties": {
        "column": {
          "type": "array",
          "description": "Column names to read from the table",
          "items": {
            "type": "string"
          },
          "minItems": 1
        },
        "where": {
          "type": "string",
          "description": "SQL WHERE clause for filtering data"
        },
        "datasource": {
          "type": "string",
          "description": "Datasource name in DataWorks"
        },
        "table": {
          "type": "string",
          "description": "Table name to read from"
        },
        "splitPk": {
          "type": "string",
          "description": "Column name used for data sharding"
        },
        "fetchSize": {
          "type": "integer",
          "description": "Number of rows fetched per batch"
        },
        "querySql": {
          "type": "string",
          "description": "Custom SQL query for reading data"
        },
        "username": {
          "type": "string",
          "description": "Database username"
        },
        "password": {
          "type": "string",
          "description": "Database password"
        },
        "jdbcUrl": {
          "type": "string",
          "description": "JDBC connection URL"
        },
        "sourceType": {
          "type": "string",
          "description": "Source type (e.g., binlog, query)"
        }
      },
      "required": [
        "column",
        "datasource",
        "table"
      ],
      "additionalProperties": true
    }
  },
  "additionalProperties": true
}
