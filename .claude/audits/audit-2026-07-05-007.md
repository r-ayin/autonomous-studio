# Deep Audit Report: shizi (re-audit)

- **audit_id**: audit-2026-07-05-007
- **project**: shizi（电商图片文字计数器 Chrome MV3 扩展）
- **module**: src/background 全栈（vision-adapter + orchestrator + ocr-internal + segmentation + counter + index）+ src/shared/storage + src/popup/{ImageUploader,ResultPanel}
- **audit_depth**: deep（再审计轮：核实 audit-2026-07-01-005 的 14 条 finding 修复状态 + 挖 evolved 代码新 bug）
- **audit_type**: code-review + security-review
- **date**: 2026-07-05
- **auditor**: autonomous-studio engine main loop（Ralph Wiggum deep-audit round, budget uncapped；sonnet sub-agent 扇出在本 model 历史 402/0-token → 直读 8 源文件 + git log + 测试验证）
- **trigger**: audit-cycle-state.status=cycle-complete（audit-2026-07-05-006 oneday-design 2/2 derived fix deadlock-archive）→ 事件驱动新全量审计
- **prior audits of this module**: audit-2026-07-01-005（14 findings: 2 HIGH + 5 MEDIUM + 4 LOW + 3 INFO）。本轮核实修复 + 挖新 bug，**不重复**已修复项的 cosmetic 细节。

## 方法

1. `git log --since=2026-07-01` 核实 9 个 fix commit（13a6988/8428be1/5476523/abcd03a/ae985a5/e8cc079/b16819f/b03935a/3f09f2f/58c71ec）落定 + 1 个新 feature commit（d360b4f VL 识别合并校对）。
2. 全文深读 8 个核心源文件：vision-adapter.ts(713L)、orchestrator.ts(308L)、ocr-internal.ts(641L)、segmentation.ts(257L)、storage.ts(93L)、background/index.ts(121L)、counter.ts(86L)、ResultPanel.tsx(166L) + ImageUploader.tsx(76L) + manifest.json。
3. `npx vitest run` 实测 53/53 全绿（1.17s）。
4. 数据流追踪：ImageUploader(processFile 透传原始字节) → background/index(runPipeline) → orchestrator(vl_text|parallel|auto|vl|local 五种 mode 分发) → vision-adapter(callVisionApi Anthropic|OpenAI 双格式) + segmentation(ONNX 本地) + ocr-internal(内网 pixelator 切片) → counter(纯 JS 等效字数)。
5. 跨模块对比：preprocessForVL 的 10MB OOM 守卫 vs segmentation/ocr-internal 的 atob 全量解码路径；nmsDedup 的 skipFuzzy 守卫 vs mergeMultiSourceResults 的无守卫 O(n²) editDistance。

## Prior Findings 修复状态核实（audit-2026-07-01-005 的 14 条）

| 旧 ID | 状态 | 证据 |
|-------|------|------|
| SH-H-001 API error 脱敏 | ✅ FIXED | vision-adapter.ts:625 `sanitizeErrorBody` 剥 Bearer/token/api-key/authorization + :643 `parseApiError` 截断≤200；callAnthropicApi:127 / callOpenAiApi:176 / testConnection:687,708 均走 parseApiError。commit 13a6988 |
| SH-H-002 vl_text 三路并行吞错 | ⚠️ **PARTIALLY FIXED** | vl_text mode (orchestrator.ts:131-149) 已用 parallelErrors[] 透传到 debugInfo.error ✓；但 **`parallel` mode:202-205 与 ocrPromise:238-241 仍 `.catch(()=>[])` 静默吞错，无 debugInfo.error** —— 见新 finding SH2-M-001 |
| SH-M-001 settings 浅合并 | ✅ FIXED | storage.ts:34 `deepMergeSettings` 三层 nested merge (vision/counter/detectionParams)。commit ae985a5 |
| SH-M-002 callOcr 无 timeout/retry | ⚠️ **PARTIALLY FIXED** | ocr-internal.ts:91 `fetchWithTimeout` + :121 `callOcr` 30s+retry1 ✓；但 **error message `OCR 请求失败: ${res.status}`(:107) 与 `图片上传失败: ${res.status}`(:67) 仍缺 body 诊断** —— 见 SH2-L-001 |
| SH-M-003 getModelBuffer fetch 卡死 | ✅ FIXED | segmentation.ts:66 `fetchBufferWithTimeout` 30s + :90 timeout retry-once + 多 mirror 串行兜底。commit abcd03a |
| SH-M-004 saveHistoryItem 阻塞 | ✅ FIXED | background/index.ts:94-99 try/catch 降级 console.warn，不阻塞 success response。commit 3f09f2f+b03935a（注：两个 commit 同一修复，轻度 churn 但功能正确） |
| SH-M-005 deletedStack 误删 | ✅ FIXED | SidePanelApp 改用 VerifiedText.id 比对。commit 58c71ec |
| SH-L-001 preprocessForVL OOM | ⚠️ **PARTIALLY FIXED** | vision-adapter.ts:296 rawLen>10MB 拒纯 ✓；但 **segmentation.ts:122 `base64ToImageData` 与 ocr-internal.ts 多处 atob 全量解码无同款守卫** —— 见 SH2-M-002 |
| SH-L-002 nmsDedup O(n²) | ⚠️ **PARTIALLY FIXED** | ocr-internal.ts:327 `skipFuzzy = sorted.length>150` 早退 ✓；但 **`mergeMultiSourceResults`:389-439（主 OCR 路径最终合并器）无同款守卫** —— 见 SH2-M-003 |
| SH-L-003 saveHistory 非原子 | ✅ FIXED | storage.ts:16 `withHistoryLock` promise-queue mutex 串行化 save/delete/clear。commit b16819f |
| SH-L-004 manifest 字符串拼接 | 🔁 structural-debt | build.mjs 仍字符串拼接（未抽 template）。留 structural-debt |
| SH-I-001 debug 脚本 tracked | 🔁 未处理 | tests/ 仍混 tracked 脚本（PROGRESS.md 自述） |
| SH-I-002 VL_KEY 明文文档 | 🔁 未处理 | PROGRESS.md:23 + GATES.md:29 仍明文 `8be079a26524bb6803161c4679040bd3`（标注已泄露待 history-rewrite，独立高危单位，需用户授权） |
| SH-I-003 模型名硬编码 | 🔁 未处理 | vision-adapter.ts:67-72 `IDEALAB_OPENAI_MODELS` 仍硬编码 4 模型名 |

**核实结论**：14 条中 7 条完全修复 ✓、3 条部分修复 ⚠️（衍生新 finding）、4 条 structural/INFO 未动（符合"不主动修 structural-debt"纪律）。无回归。

---

## 新 Findings

| ID | Severity | File | Line | Kind | 简述 |
|----|----------|------|------|------|------|
| SH2-M-001 | MEDIUM | src/background/orchestrator.ts | 201-205, 238-241 | route-fix | SH-H-002 修复不完整：`parallel` mode 的 detectProductRegion+segmentProduct 双 `.catch(()=>[])` 与 ocrPromise 的 VL 识别 catch 均静默吞错，不写 debugInfo.error、不置 detectFailed；用户在 parallel/auto+enableVerify mode 下遇到检测/识别失败会看到"0 区域/0 文字"假阴性，无任何错误信号——正是 SH-H-002 在 vl_text mode 修掉的反模式在其余 mode 残留 |
| SH2-M-002 | MEDIUM | src/background/segmentation.ts | 122-136 | route-fix | SH-L-001 修复不完整：`base64ToImageData` 对 full base64 做 `atob(pure)`+`Uint8Array(binary.length)` 全量解码，无 >10MB 守卫；segmentProduct 在 vl_text/parallel/local/auto mode 均接收**原始 imageData**（非 preprocessForVL 产物），>10MB 图直接 atob OOM 触发 service worker 崩溃。SH-L-001 守卫只保护 VL 路径，segmentation 路径全裸 |
| SH2-M-003 | MEDIUM | src/background/ocr-internal.ts | 389-439 | route-fix | SH-L-002 修复不完整：`mergeMultiSourceResults`（主 OCR 路径 whole+tiled+enhanced 三源最终合并器）双层循环对每对结果调 `textSimilarity`→`editDistance` O(n*m) DP，**无 skipFuzzy 早退守卫**（nmsDedup 有 :327 守卫但本函数没有）；密集文字图（>150 条跨源结果）触发 ~22k+ 次 editDistance，service worker 主线程卡顿——SH-L-002 修了 nmsDedup 却漏了同款 O(n²) 的姊妹函数 |
| SH2-M-004 | MEDIUM | src/background/vision-adapter.ts | 114, 161 | route-fix | `callAnthropicApi` 硬编码 `media_type:'image/png'`(:114)，`callOpenAiApi` 硬编码 `data:image/png;base64,`(:161)；但 (a) ImageUploader.tsx:21 透传原始字节不转 PNG，用户上传 JPEG/WebP 即 mislabeled；(b) preprocessForVL:317 与 compressFallback:343 把大图重编码为 **JPEG**，仍贴 image/png 标签。Anthropic API 严格校验 media_type vs 字节 → 任意非 PNG 上传或任意 >1.5MB/2500px 大图在 Anthropic 路径触发 400，且 400 触发的 compressFallback 再产 JPEG 仍 mislabeled → 死循环 400 → throw，大图分析硬失败 |
| SH2-L-001 | LOW | src/background/ocr-internal.ts | 67, 107 | route-fix | SH-M-002 修复不完整：`callOcrOnce`(:107) `OCR 请求失败: ${res.status}` 与 `uploadImage`(:67) `图片上传失败: ${res.status}` 仍不读 response body；内网 pixelator 4xx/5xx 时缺诊断文本，排障靠盲猜。Fix: `await res.text()` 截断≤200 拼进 error message（与 parseApiError 同思路） |
| SH2-L-002 | LOW | src/popup/ResultPanel.tsx | 56, 62 | route-fix | 分类明细行"→ 等效X"用硬编码 `Math.ceil(count/2)` 重算，未读 config.englishRatio/digitRatio；当用户在 settings 配 ratio=3 时，equivalentCount 用 3 算、明细行用 /2 显示，行级等效累加≠总等效字数，UI 自相矛盾。Fix: 把 config 透传 ResultPanel 或在 CountResult 里带 per-row equiv |
| SH2-L-003 | LOW | src/background/vision-adapter.ts | 567-618 | route-fix | `verifyOcrResults` 全仓零调用者（src/ tests/ grep 无 caller）= 死代码；且其 :579 `callVisionApi(config, prompt, imageBase64)` 用**未预处理**的原始 imageBase64，无 preprocessForVL 守卫、无 compressFallback 400 兜底——一旦未来被重新接回 enableVerify=false 路径，大图直接 OOM/400 硬失败。Fix: 删除（VL 识别已合并校对，此函数被 d360b4f 取代）或补预处理+兜底 |
| SH2-I-001 | INFO | PROGRESS.md / GATES.md / vision-adapter.ts:67 | 23,29,67-72 | structural | 旧 structural/INFO 项延续：VL_KEY 明文文档未脱敏(SH-I-002)、IDEALAB_OPENAI_MODELS 硬编码(SH-I-003)、manifest 字符串拼接(SH-L-004)。均已在旧审计登记，本轮仅确认状态未变，不重复派 fix |

**统计**: MEDIUM 4 / LOW 3 / INFO 1 = 8 new findings

## 优先派生 fix 建议（按 severity + impact 排序）

1. **SH2-M-004** media_type 硬编码 → route-fix，vision-adapter.ts 单文件：加 `detectMediaType(base64)`（嗅探 PNG/JPEG/WebP magic bytes）+ callAnthropicApi/callOpenAiApi 用动态 media_type。也可顺带修 SH2-L-003（删 verifyOcrResults）。**最高优先**——影响所有 JPEG/WebP 上传 + 所有大图。
2. **SH2-M-002** segmentation OOM 守卫 → route-fix，segmentation.ts 单文件：base64ToImageData 入口加 rawLen>10MB 早退 throw IMAGE_TOO_LARGE（与 preprocessForVL 同款）。
3. **SH2-M-003** mergeMultiSourceResults perf 守卫 → route-fix，ocr-internal.ts 单文件：复用 nmsDedup 的 `skipFuzzy = all.length>150` 早退 + IoU≤0.1 continue。
4. **SH2-M-001** orchestrator 并行吞错补全 → route-fix，orchestrator.ts 单文件：把 vl_text mode 的 parallelErrors[] 模式复制到 parallel mode(:201-205) 与 ocrPromise catch(:238-241)，写 debugInfo.error。
5. **SH2-L-001** OCR/上传错误 body 诊断 → route-fix，ocr-internal.ts 单文件：:67/:107 补 `await res.text()` 截断≤200。
6. **SH2-L-002** ResultPanel 等效显示用 config → route-fix，ResultPanel.tsx + 透传 config（可能触及 2 文件）。
7. **SH2-L-003** 删 verifyOcrResults 死代码 → route-fix，vision-adapter.ts 单文件（可与 SH2-M-004 同 worktree）。

## 不在本轮范围

- VL_KEY history-rewrite（SH-I-002，独立高危单位，需用户显式授权 + 平台侧 key 轮换）
- manifest template 抽离（SH-L-004 structural-debt，需讨论 build 管线变更）
- tests/ debug 脚本迁移（SH-I-001，信噪比优化，非功能 bug）

## 派生 fix 登记（写入 audit-cycle-state.derived_fixes，status=pending）

- SH2-M-004 → case TBD, kind=route-fix, files=[vision-adapter.ts], priority=1
- SH2-M-002 → case TBD, kind=route-fix, files=[segmentation.ts], priority=2
- SH2-M-003 → case TBD, kind=route-fix, files=[ocr-internal.ts], priority=3
- SH2-M-001 → case TBD, kind=route-fix, files=[orchestrator.ts], priority=4
- SH2-L-001 → case TBD, kind=route-fix, files=[ocr-internal.ts], priority=5
- SH2-L-002 → case TBD, kind=route-fix, files=[ResultPanel.tsx], priority=6
- SH2-L-003 → case TBD, kind=route-fix, files=[vision-adapter.ts], priority=7（可与 #1 同 worktree）

下轮起逐个派生最小 fix case（route-fix 复用 area worktree，不触及公共接口文件）。
