# audit-2026-07-02-003 — skills/runner-exec 深度安全+正确性审计

- **审计时间**: 2026-07-02T03:25Z
- **项目**: skills
- **模块**: runner-exec (scripts/batch-write.js, creds.js, exec.js, pull.js, push.js, session.js, write-file.js)
- **触发原因**: cycle-complete → 新全量审计；skills #1 health score 6.09，runner-exec 安全敏感度高（凭证+远程执行）且未审过
- **审计范围**: runner-exec/scripts/ 全部 7 个 JS 文件
- **审计人**: autonomous-studio ralph-wiggum loop (deep-audit, sub-agent model=sonnet)

## 摘要

18 findings (5 high, 6 medium, 5 low, 2 info)。核心风险：所有 PowerShell 单引号路径拼接均未转义单引号字符导致命令注入（SK-RE-002~005），creds.js execSync shell 拼接存在 HOME 变量注入风险（SK-RE-008）。中等问题包括 token 明文 URL 传输、超时静默截断、error handler 未清 timer。低级问题包括路径遍历、MD5 碰撞、escapePS 重复定义和并发安全。

## Findings

### High Severity (5)

| ID | File:Line | Finding | Kind |
|----|-----------|---------|------|
| SK-RE-001 | scripts/exec.js:23 | 命令注入 — execOnRunner 直接将 command 参数拼进 WebSocket input 发送到远程 PowerShell，无任何过滤或转义。设计即如此但应在文档明确标注。 | structural |
| SK-RE-002 | scripts/write-file.js:28 | 路径注入 — windowsPath 直接拼入 PowerShell 单引号字符串，单引号未转义。含单引号的路径可截断引号执行任意命令。 | route-fix |
| SK-RE-003 | scripts/batch-write.js:28 | 路径注入 — f.path 直接拼入 PowerShell 单引号字符串，与 SK-RE-002 相同漏洞。batch-write 从外部 manifest.json 读取路径，攻击面更大。 | route-fix |
| SK-RE-004 | scripts/push.js:60 | 路径注入 — f.winPath 和 parent 直接拼入单引号 PowerShell 字符串，与 SK-RE-002/003 相同问题。 | route-fix |
| SK-RE-005 | scripts/pull.js:41 | 路径注入 — winDir 和 winPath 拼入单引号 PowerShell 字符串（行 41 Get-ChildItem 和行 65 Get-Content）。 | route-fix |

### Medium Severity (6)

| ID | File:Line | Finding | Kind |
|----|-----------|---------|------|
| SK-RE-006 | scripts/creds.js:11 | 凭证读取无权限校验 — getInternalSecret 读取 secret 文件后直接返回明文，未检查文件权限是否 0600。 | route-fix |
| SK-RE-007 | scripts/creds.js:16 | pgrep 模式 'node.*cloud-cli.*server' 过宽，可能匹配非目标进程读取错误 secret。 | route-fix |
| SK-RE-008 | scripts/creds.js:29 | SQL/命令注入 — dbPath 通过 shell 字符串拼接传给 execSync(`sqlite3 "${dbPath}" ...`)，HOME 被篡改可导致注入。应改 execFileSync 数组形式。 | route-fix |
| SK-RE-009 | scripts/session.js:18 | WebSocket URL 中 token 以 query string 明文传输（4 处），可能泄露到日志/代理记录。 | structural |
| SK-RE-010 | scripts/exec.js:38 | WebSocket error 事件 reject 但未 clearTimeout，timer 泄漏可能导致 ws.close() 在已关闭 socket 上调用。 | route-fix |
| SK-RE-011 | scripts/session.js:38 | timeout 分支只 resolve 不 reject，调用方无法区分正常完成和超时截断。 | route-fix |

### Low Severity (5)

| ID | File:Line | Finding | Kind |
|----|-----------|---------|------|
| SK-RE-012 | scripts/pull.js:60 | 路径遍历 — rel 由远程路径减前缀得到，含 ../ 可写入 localDir 之外。 | route-fix |
| SK-RE-013 | scripts/push.js:7 | escapePS 函数重复定义（push.js 和 write-file.js 各一份），应统一导入。 | structural |
| SK-RE-014 | scripts/write-file.js:5 | escapePS 未转义 NUL 字节和控制字符，PowerShell Set-Content 行为不可预测。 | route-fix |
| SK-RE-015 | scripts/pull.js:71 | MD5 哈希做文件变更检测，应改 sha256。 | route-fix |
| SK-RE-016 | scripts/session.js:48 | RunnerSession 不支持并发 exec，_onData 回调被覆盖导致前者永不 resolve。 | structural |

### Info Severity (2)

| ID | File:Line | Finding | Kind |
|----|-----------|---------|------|
| SK-RE-017 | scripts/batch-write.js:55 | manifest.json 无 schema 校验，缺字段产生 undefined 拼入 PowerShell。 | route-fix |
| SK-RE-018 | scripts/creds.js:14 | catch {} 空块静默吞异常，丢失根因信息增加排障难度。 | route-fix |

## 修复优先级建议

1. **P0 (立即)**: SK-RE-002~005 单引号路径注入 — 统一 escapePS 并应用到所有拼接点
2. **P1 (本周)**: SK-RE-008 execSync→execFileSync, SK-RE-010 timer cleanup, SK-RE-011 timeout reject
3. **P2 (计划内)**: SK-RE-006 文件权限校验, SK-RE-012 路径遍历防护, SK-RE-013 escapePS 去重
4. **P3 (改善)**: SK-RE-009 token 传输方式, SK-RE-015 MD5→SHA256, SK-RE-016 并发队列
5. **Info**: SK-RE-017 schema 校验, SK-RE-018 错误信息保留
