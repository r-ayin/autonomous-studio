# audit-2026-07-01-011 · 1BfrYW9R (oneday-design) · deep audit

- **audit_id**: audit-2026-07-01-011
- **project**: 1BfrYW9R (oneday-design — personal-assistant UI, React single-file bundle + Node dev-server + static build)
- **module**: dev-server + build + smoke-test (全量三文件)
- **depth**: deep
- **completed_at**: 2026-07-01T23:55:00Z
- **auditor**: autonomous-engine (ralph wiggum cycle, ultracode)

## 范围与方法

单轮深审三个核心源文件（build.js 79L / dev-server.js 158L / test/smoke.test.js 2330L）+ CLAUDE.md/README/package.json。追踪跨模块数据流：HTTP req → method guard → path normalize → probe routes (/healthz /readyz /api/health) → static dispatch → MIME → response headers; build: index.html parse → inline babel regex → component count → ref resolution; smoke: spawn server → poll → assert.

## Findings 汇总

| ID | file | line | severity | kind | finding | remediation |
|---|---|---|---|---|---|---|
| OD-001 | dev-server.js | 100-116 | medium | route-fix | `/api/health` 反代未校验上游响应 Content-Type，body 直接透传。若 ONEDAY_API_BASE 指向的上游被替换为恶意服务返回 `text/html` + XSS payload，dev-server 会以自己 origin 把 HTML 渲染给浏览器——同源策略绕过演化为 SSRF-to-XSS。当前 nosniff 头只防 MIME sniffing，不拦 text/html 主动声明的脚本执行。 | 在 upstream 'end' handler 内检查 `up.headers['content-type']`：非 application/json 一律按 bad gateway 处理（或强制覆写为 application/json 后再 end body）。最小改动 1 处，route-fix。 |
| OD-002 | dev-server.js | 100-114 | low | route-fix | `/api/health` 反代无 body size cap，恶意/故障上游可灌入 GB 级响应耗尽 dev-server 内存（b += c 字符串拼接无限增长）。生产入口 npm start 暴露此面。 | 累加 b.length，超阈值（如 1MB）调 fail('upstream response too large')。route-fix，与 OD-001 同文件可合并提交。 |
| OD-003 | build.js | 45 | info | structural | 组件计数正则为手写启发式，class/function/const arrow/React.memo 四种形态硬编码；新增 class field 组件、forwardRef、memo-wrapped arrow 等形态会漏计。当前靠 ⑧ 文档对账兜底，但文档也错时静默通过（注释自述 2026-06-30 class 漏计历史）。 | 长期方向：引入 @babel/parser 做 AST 计数取代正则。structural-debt 登记，不在本轮派 fix。 |
| OD-004 | test/smoke.test.js | 全文件 | info | none | 测试套件防御极强（30+ 测覆盖 method/path/probe/MIME/SIGTERM/SIGINT/HEAD/query/traversal/doc-drift/class-def/port-range/gates-doc），但缺 `/api/health` 反代行为测（OD-001/OD-002 零覆盖）——反代路径是当前唯一未被咬的服务端代码路径。 | OD-001 fix 时同步补负测：注入 mock upstream 返 text/html → 断言 dev-server 不转发该 body。 |
| OD-005 | dev-server.js | 19 | info | none | ONEDAY_DIR env 允许任意路径注入作为 serve root。本地 dev 合理；生产部署若 PaaS 误注 ONEDAY_DIR 为敏感目录（/etc /root）会把系统文件暴露给 HTTP。已有路径穿越防御保护「不泄露 serve root 之外」，但 serve root 本身可被改指。 | 文档化 ONEDAY_DIR 仅用于测试；或在 production mode（如 NODE_ENV=production）禁掉该 env。info 级，不阻塞。 |

**总计**：5 findings（medium 1 / low 1 / info 3）。派生 route-fix 2 条（OD-001 + OD-002 可合并为一个 workunit，均 dev-server.js）；structural 1 条（OD-003 登记 debt）；info 2 条不派 fix。

## 审计结论

1BfrYW9R 是本次审计周期内防御最完善的项目之一：路径穿越/方法守卫/端口范围/探针语义/MIME 派发/SIGTERM-SIGINT 优雅关闭/HEAD 放行/query 剥离/目录 404/文档漂移/class 组件计数 全部有回归锁咬住。注释密度极高、每条防御都引证对应测试编号形成双向追溯。唯一真实安全缺口在 `/api/health` 反代路径（OD-001/OD-002）——这是 Phase 1 groundwork 预留的同源反代通道，目前只接 /healthz 探针故攻击面窄，但一旦接入真实 API 即放大。建议优先修。

## 派生动作

- OD-001 + OD-002 → 合并派生 1 个 route-fix case（dev-server.js 单文件改动 + 同步补 smoke test），kind=route-fix，走 opt-worktree 复用规则。
- OD-003 → 登记 `.claude/structural-debt.md`（AST parser 替代正则计数），不派 fix。
- OD-004/OD-005 → info，仅在 NEXT_SUGGESTION 提醒，不建债务条目。
