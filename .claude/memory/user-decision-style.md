---
name: user-decision-style
description: 从用户会话 transcript 蒸馏的决策风格契约 — 由 distill-user-style.py 自动同步
metadata:
  type: project
---

# 用户决策风格契约

> 引擎 Step0 必读，与 autonomous-constraints.md 同权重遵守。
> 原料：~/.claude/projects/-home-admin-workspace/*.jsonl 的 type=user 行（110 会话/903 条去重输入）。
> 规则须有≥2条独立用户输入支撑（孤证不取）。evidence 引用用户原话片段，非 LLM 臆测。
> 最后蒸馏：2026-07-06T02:41:23Z

## 沟通粒度与汇报偏好
### Style: 要求完整详细不加省略的报告并核实事实
- **签名前缀**: `comm:要求完整详细不加省略的报告并核实事实`
- **规则**: 输出须完整、详细、不省略；对关键结论要求事实确认与验证说明，不接受模糊或断言式结论
- **evidence**: 「输出完整详细不加省略的错误报告」 / 「介绍一下具体是怎么验证的？」 / 「确认以下几个事实」
- **confidence**: 0.33

### Style: execute-ordered-steps
- **签名前缀**: `comm:execute-ordered-steps`
- **规则**: 用户常以"建议按以下顺序/步骤修复"+编号步骤的形式下达修复指令，期望逐字执行而非自由发挥，步骤即契约
- **evidence**: 「建议按以下顺序修复」 / 「建议按以下步骤修复」 / 「操作步骤」
- **confidence**: 0.62

### Style: plain-status-and-gap
- **签名前缀**: `comm:plain-status-and-gap`
- **规则**: 无法容忍听不懂的术语堆砌，要求用大白话回答"现在还差啥/重构了什么"，结论先行并明确剩余缺口
- **evidence**: 「我怎么听不懂你说什么？现在还差啥？还有这个重构版相比原项目都重构了什么？」 / 「有什么需要我确认的」
- **confidence**: 0.42

### Style: 要求全面展开而非最小极限
- **签名前缀**: `comm:要求全面展开而非最小极限`
- **规则**: 汇报与方案要全面展开、细致到最小颗粒度，明确反对「最小极限/最小化」做法，要求一次性给全信息
- **evidence**: 「不要最小极限，我要最全面的一次性做到最全省的后面再数据缺失」 / 「先落盘全面的升级后的详细方案，细致到最小颗粒度，然后进行校对，再开干」 / 「补充所有的信息，可以给表格增加列」
- **confidence**: 0.27

### Style: 术语须用业务语言解释
- **签名前缀**: `comm:术语须用业务语言解释`
- **规则**: 对使用的指标/术语要求用业务语言解释含义与区别，不接受黑话或纯技术定义
- **evidence**: 「点问题你给我解释清楚你用的术语，用业务语言来说」 / 「维度级异常率和答案熵有什么区别？答案分布 KL 散度 vs 群体具体是什么意思」
- **confidence**: 0.18

### Style: 要求因果解释而非仅结论
- **签名前缀**: `comm:要求因果解释而非仅结论`
- **规则**: 对异常检测结果要求给出因果解释——为什么命中、为什么漏掉、为什么多识别，而非仅给名单
- **evidence**: 「之前测出来的肯定有问题的7个人里哪个检测到了，是为什么检测到...哪个没检测到，又是为什么漏掉了？哪个是多识别的」 / 「按照你的建议，然后再排查一下，为什么自动决策引擎只修自己」
- **confidence**: 0.18

## 确认门槛与自动化边界
### Style: 方案先审执行可委托不可逆动作须用户明确指示
- **签名前缀**: `gate:方案先审执行可委托不可逆动作须用户明确指示`
- **规则**: 涉及重构/方案类工作先给完整方案待审，常规执行可委托 agent；但推送、删除、合并远端等不可逆动作须按用户明确指示进行
- **evidence**: 「先给我详细完整的方案」 / 「全部由你执行确认」 / 「不要合并远端改动，退回，只推送你改的」
- **confidence**: 0.33

### Style: 倾向全自动消除人工干预
- **签名前缀**: `gate:倾向全自动消除人工干预`
- **规则**: 在已授权的运维/导出类场景中要求全自动运行，消除人工干预
- **evidence**: 「完全不需要人工干预」 / 「必须全自动」
- **confidence**: 0.22

### Style: auto-push-branches-not-main
- **签名前缀**: `gate:auto-push-branches-not-main`
- **规则**: worktree/分支上的改动必须自动 push，但禁止自动 push 到主干；构建完成后才 push
- **evidence**: 「全部push，必须自动push到worktree分支」 / 「所有分支操作都应该自动push的」 / 「只是不能自动push到主干上」
- **confidence**: 0.7

### Style: auto-advance-sync-key-nodes
- **签名前缀**: `gate:auto-advance-sync-key-nodes`
- **规则**: 倾向全自动推进、关键节点同步用户；可自动触发的迁移类操作（如图片上云）应无感自动完成，不留手动按钮
- **evidence**: 「A) 全自动推进，关键节点同步你（推荐）」 / 「启动自动决策引擎的自动模式」 / 「图片的迁移到云不要手动触发，而是保存文件编辑时自动无感触发」
- **confidence**: 0.68

### Style: 引擎启停由用户显式控制
- **签名前缀**: `gate:引擎启停由用户显式控制`
- **规则**: 自动决策引擎的启停与重大变更须由用户显式下达，变更前先停引擎并全面审查其产出再决定合并
- **evidence**: 「先停引擎，然后全部对比一下，全部都合」 / 「先暂停自动决策引擎，然后你全面审查他产出的工作。之后有修复的就直接合并到主干上」
- **confidence**: 0.25

### Style: 动手前先落盘完整方案并校对
- **签名前缀**: `gate:动手前先落盘完整方案并校对`
- **规则**: 执行前先等真实数据/落盘完整计划并校对，形成完整方案后再开干，不抢先动手
- **evidence**: 「现在先不要查题库，等我导入真实的作业数据。记录好，形成完整的计划」 / 「先落盘全面的升级后的详细方案，细致到最小颗粒度，然后进行校对，再开干」
- **confidence**: 0.25

## 风险偏好与节奏
### Style: 修复优先最小改动不擅自重构
- **签名前缀**: `risk:修复优先最小改动不擅自重构`
- **规则**: 遇故障先修复成可用状态，按指定版本/范围做最小改动，不擅自重构页面或扩大改动面
- **evidence**: 「不重构页面」 / 「先修复成可用的」
- **confidence**: 0.33

### Style: full-fix-not-partial
- **签名前缀**: `risk:full-fix-not-partial`
- **规则**: 修复倾向"全量/全修"一次性覆盖，而非逐条零敲碎打；常配合合并积压分支后启动引擎
- **evidence**: 「全量修复」 / 「全修，然后合并积压的opt-worktree，然后启动自动引擎」 / 「全量修复，按照1d平台适配技能，审计修复情况」
- **confidence**: 0.66

### Style: audit-then-resolve-cadence
- **签名前缀**: `risk:audit-then-resolve-cadence`
- **规则**: 偏好深度审计定位 bug，但审计频率要随进度下调：解决完一轮问题再做下一轮全面审计，避免空转审计
- **evidence**: 「进行深度审计，排查bug和逻辑舞曲」 / 「全量审计排查一下」 / 「降低全量审计频率，而是根据进度，解决完一次审计的问题再进行下一次全面审计」
- **confidence**: 0.6

### Style: reuse-worktree-unless-direction-shift
- **签名前缀**: `risk:reuse-worktree-unless-direction-shift`
- **规则**: opt-worktree 默认复用，仅在"方向性更新"时才新建；需区分原路线修复与项目方向变更，不要为小修复频繁开新 worktree
- **evidence**: 「opt-worktree只在实际改动方向大时才创建新的，不然引擎会一直复用一个」 / 「真正应该调整的是你怎么判定是原有路线上的修复，还是项目方向上的更新」
- **confidence**: 0.45

### Style: isolate-refactor-new-dir
- **签名前缀**: `risk:isolate-refactor-new-dir`
- **规则**: 重构类工作不得改动原项目代码，须新建独立目录承载，避免污染既有可运行项目
- **evidence**: 「不要改我的原项目代码，而是新创建一个fa_agent项目目录」 / 「我要把这个项目新创建一个目录，这个目录可以摒除一切关于1d 的适配，全面重构」
- **confidence**: 0.5

### Style: 偏好彻底全面而非MVP
- **签名前缀**: `risk:偏好彻底全面而非MVP`
- **规则**: 明确反 MVP/最小化，偏好一次性彻底做到位，遇到错误倾向推倒重来/重建而非打补丁
- **evidence**: 「不要最小极限，我要最全面的一次性做到最全省的后面再数据缺失」 / 「B（彻底）：重建」 / 「这两个删掉...重新从源文件进行提取」
- **confidence**: 0.3

### Style: 修复后逐条逐个实测验证
- **签名前缀**: `risk:修复后逐条逐个实测验证`
- **规则**: 修复完成后必须用实际数据逐条逐个测试验证，不信任批量脚本的自检结论
- **evidence**: 「然后并进行再修改之后并进行逐条逐个的实际数据测试」 / 「现在题目信息还是错的，你一个一个手动提取，不要依赖脚本」
- **confidence**: 0.2

## 技术选型与实现倾向
### Style: 禁止硬编码凭据与模型id须配置化
- **签名前缀**: `tech:禁止硬编码凭据与模型id须配置化`
- **规则**: MCP、ak、模型 id 等不得硬编码进代码，须走环境配置/占位符注入
- **evidence**: 「不要把我的mcp写在代码里」 / 「取消里面的硬编码」
- **confidence**: 0.17

### Style: 模型选型成本敏感倾向多模型综合而非单一最优
- **签名前缀**: `tech:模型选型成本敏感倾向多模型综合而非单一最优`
- **规则**: 关注模型调用成本，倾向多模型综合（相同点+不同点判断排除错误）并要求来源标注，而非只取单一最优输出
- **evidence**: 「现在默认都是opus太贵了」 / 「将所有模型的答案做综合」 / 「4个模型都判断之后再综合吗？怎么没有来源标注了？」
- **confidence**: 0.25

### Style: 数据须落云端存储禁止本地缓存兜底
- **签名前缀**: `tech:数据须落云端存储禁止本地缓存兜底`
- **规则**: 业务数据须持久化到云端存储（OneDay Cloud/Dolt 等），禁止缓存或本地离线兜底
- **evidence**: 「持久化留存在1d cloud的云端储存中，而不是存在缓存或本地」 / 「文档直接上传到knowledge-files云储存桶中」
- **confidence**: 0.17

### Style: 禁止降级与缓存兜底行为
- **签名前缀**: `tech:禁止降级与缓存兜底行为`
- **规则**: 禁止任何降级到缓存/本地的兜底逻辑，遇服务不可用直接报错而非静默降级
- **evidence**: 「禁止进行任何降级」 / 「不保留任何本地数据作为离线降频」
- **confidence**: 0.17

### Style: standardize-glm52-with-fallback
- **签名前缀**: `tech:standardize-glm52-with-fallback`
- **规则**: 全局默认模型统一为 GLM-5.2（开发与自动引擎均如此），禁用其他模型；429 时自动切 qwen3.7-max 兜底
- **evidence**: 「将自动决策引擎的默认模型全局设定为GLM-5.2」 / 「开发全用GLM-5.2来进行，不用其他模型」 / 「如果自动引擎的glm5.2报429，就自动切换到qwen3.7-max」
- **confidence**: 0.72

### Style: dedup-redundant-deps
- **签名前缀**: `tech:dedup-redundant-deps`
- **规则**: 厌弃重复/嵌套依赖：同一能力的重复实现（shizi=text-counter-extension、重复视觉模型、Vditor 在 1d 不支持）应彻底删除并统一到单一来源
- **evidence**: 「去掉shizi视觉模型的依赖，统一靠当前环境的视觉模型来完成，不需要重复嵌套模型」 / 「text-counter-extension就是shizi，检查下有什么不同？另一个可以删掉了」 / 「把Vditor方案彻底删掉吧，1d不支持这个」
- **confidence**: 0.66

### Style: ecs-dolt-self-host
- **签名前缀**: `tech:ecs-dolt-self-host`
- **规则**: 后端优先自托管到 ECS：复用 ECS 上的 Dolt 数据库、ECS IP 直发、直连 deepseek，规避 1d/Supabase 平台限制
- **evidence**: 「复用ecs上的dolt数据库，全部移植发布到ecs服务器上」 / 「用 ECS IP 直发」 / 「全部搬迁到 ECS Dolt」
- **confidence**: 0.55

### Style: single-source-of-truth
- **签名前缀**: `tech:single-source-of-truth`
- **规则**: 数据/权限须单一来源（如 Cloud staff 表为唯一人员权限源），禁止本地 JSON+Cloud 双写造成双数据源与不一致
- **evidence**: 「Cloud staff 表是唯一人员与权限来源」 / 「不建议把"应用内人员管理"先写云函数本地 JSON，再同步 Cloud；这样会出现双数据源」
- **confidence**: 0.5

### Style: 前端体验优先所见即所得
- **签名前缀**: `tech:前端体验优先所见即所得`
- **规则**: 前端体验优先，偏好所见即所得的渲染编辑与固定下来的好看模板，UI 先行
- **evidence**: 「md文档的渲染和编辑插件换成所见即所得的」 / 「用disagn找个好看的前端模板案例，然后固定下来」 / 「先做ui，然后检查从1d迁移过来用不了的ui」
- **confidence**: 0.38
