#!/bin/bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
COMMON_FILE="${SCRIPT_DIR}/lib/memory-common.sh"
if [ ! -f "$COMMON_FILE" ]; then
  COMMON_FILE="${SCRIPT_DIR}/memory-lib/memory-common.sh"
fi
source "$COMMON_FILE"

memory_main "$@"
