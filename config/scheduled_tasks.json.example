{
  "tasks": [
    {
      "id": "l2-heartbeat",
      "cron": "*/7 * * * *",
      "prompt": "AUTONOMOUS_HEARTBEAT L2 (7min·执行轨) v6.2 — Agent Isolation Mode:\n0. 预检: bash .claude/scripts/studio-precheck.sh . 2>/dev/null；若输出含 skip 或无活跃 Studio/目标 → 输出 \"<!-- HB SKIP -->\" 并 STOP（不 spawn agent，省 token）。\n1. 读 .claude/memory/autonomous-state.md → GOAL_STATUS 为 paused → \"<!-- HB SKIP: paused -->\" STOP；achieved 或无目标 → 继续（瞭望模式）。\n2. 预检通过 → 用 Agent 工具 spawn 子代理（subagent_type: general-purpose, description: \"L2 scout scan\"），prompt: \"You are the autonomous-studio v6.2 isolated decision agent. FIRST Read ~/.claude/skills/autonomous-studio/decision-agent-prompt.md — 那是你的完整操作手册（上下文干净，无会话历史）。Activation: L2 heartbeat 7min. 读 .claude/decision-log.jsonl 最后 30 行、.claude/decisions/calibration.json、.claude/memory/autonomous-state.md。无活跃目标 → 跑 §1.5 主动扫描协议；有活跃目标 → 跑完整 §1 七阶段框架。★ 自治优化纪律: 改动用 bash .claude/scripts/opt-worktree.sh commit <area:sub> '<说明>' 提交（禁直接 commit main——autonomous-commit-gate 会拦）；汇报含 opt-worktree.sh list 的待审 worktree 数。Output <decision>JSON</decision> + 最多 3 行摘要。\"\n3. 子代理返回 action_level ≥ ACT_NOTIFY 且 confidence ≥ 71 → 执行推荐操作；SUGGEST → 追加到 .claude/memory/autonomous-suggestions.md。\n4. 无事可做 → 输出 \"<!-- HB OK -->\" 即止。按「心跳微信路由」记忆：有实质产出时发微信，终端保持静默。",
      "recurring": true
    },
    {
      "id": "l3-deep-check",
      "cron": "43 * * * *",
      "prompt": "AUTONOMOUS_HEARTBEAT L3 (60min·研判轨) v6.2 — Agent Isolation Mode:\n1. 用 Agent 工具 spawn 子代理（subagent_type: general-purpose, description: \"L3 deep check\"），prompt: \"You are the autonomous-studio v6.2 isolated decision agent. FIRST Read ~/.claude/skills/autonomous-studio/decision-agent-prompt.md。Activation: L3 deep check. 任务: (a) 读 .claude/decision-log.jsonl 全文找模式 (b) bash: python .claude/scripts/distill-patterns.py --project . --apply（确定性蒸馏，禁手改 patterns.md——patterns-write-gate 会拦）(c) 跑 §1.5 主动扫描: git status、各项目健康、路线健康度（python .claude/scripts/route-health-scorer.py 如可用）(d) 引擎自检: 全局 ~/.claude/settings.json 含 decision-observer？CronCreate L2/L3 存活？Windows 计划任务 AutonomousStudioWatchdog 存活（schtasks 查询）？缺失→按 .claude/HOOKS.md 自检规则重建 (e) 未归档 case-*.json → 汇总进 decision-archive.md。★ 自治优化纪律: 改动走 bash .claude/scripts/opt-worktree.sh commit，禁直接 commit main。Output <decision>JSON</decision> + 最多 3 行摘要，无发现则 action_level=OBSERVE。\"\n2. 无重大发现 → 输出 \"<!-- L3 OK -->\" 即止。有实质发现按「心跳微信路由」记忆发微信。\n3. 注意: durable 定时任务 7 天自动过期——若本次是过期前最后一跳，先用 CronCreate 重建 L2/L3 再执行。",
      "recurring": true
    },
    {
      "id": "distill-patterns",
      "cron": "13 */6 * * *",
      "prompt": "AUTONOMOUS_DISTILL v6.2: 确定性蒸馏（零 LLM 推理）。执行 bash: python .claude/scripts/distill-patterns.py --project . --apply 2>&1 | tail -25。然后一行汇报: 重算了几个 pattern accuracy、几个僵尸(actions=0)、几个新注册、门禁是否通过。只执行脚本+汇报，脚本报错则输出错误并 STOP。",
      "recurring": true
    }
  ]
}